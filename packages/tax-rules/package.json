{
  "name": "@wf/tax-rules",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned payroll tax rulesets per financial year per jurisdiction, plus the rule functions that read them.",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./rulesets/*": "./rulesets/*"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
